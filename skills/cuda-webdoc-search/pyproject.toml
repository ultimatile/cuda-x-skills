[project]
name = "cuda-webdoc-search"
version = "0.0.0"
requires-python = ">=3.11"
dependencies = ["requests", "beautifulsoup4", "rapidfuzz", "sphobjinv"]

[dependency-groups]
test = ["pytest", "pytest-cov"]
